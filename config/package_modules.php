<?php

/**
 * منبعِ حقیقت (Single Source of Truth) برای نگاشت «پکیج‌ها» به «ماژول‌های فعال».
 *
 * نکات بسیار مهم:
 * 1) کلیدهای آرایه = اسلاگِ پکیج‌ها (دقیقاً همان چیزی که در DB/سیستم فروش به‌عنوان package.slug ذخیره می‌شود).
 * 2) مقدار هر کلید = آرایه‌ای از اسلاگِ ماژول‌هایی که با خرید آن پکیج باید فعال شوند.
 * 3) «حسابداری پیشرفته» فقط و فقط با اسلاگِ ماژول `accounting-advanced-qr` فعال می‌شود.
 * 4) پکیج‌های پایه/استاندارد/حرفه‌ای هرگز «پیشرفته» را فعال نمی‌کنند مگر این‌که در همین فایل صراحتاً اضافه شود.
 * 5) اگر به‌صورت مستقیم «خرید ماژول» انجام شود (نه پکیج)، این فایل نقشی ندارد؛
 *    اما سرویس فعال‌سازی شما باید همان اسلاگِ ماژول را مستقیماً فعال کند.
 *
 * ماژول‌های استفاده‌شده در این نگاشت:
 * - core
 * - announcements
 * - polls
 * - reserve
 * - meeting
 * - documents
 * - accounting-advanced-qr   ← فقط برای پکیج‌های پیشرفته
 *
 * در صورت نیاز به افزودن پکیج/ماژول‌های جدید، فقط در همین فایل اضافه کنید.
 */

return [

    // ───────────────────────────────────────────────────────────
    // پکیج‌های عمومی سایت
    // ───────────────────────────────────────────────────────────

    // «پایه» — حداقل امکانات
    'basic' => [
        'core',
        'announcements',
        'polls',
        // توجه: عمداً هیچ حسابداری‌ای (به‌خصوص پیشرفته) اینجا فعال نمی‌شود.
    ],

    // «استاندارد» — امکانات پایه + رزرو و جلسه
    'standard' => [
        'core',
        'announcements',
        'polls',
        'reserve',
        'meeting',
        // عمداً هیچ حسابداری پیشرفته‌ای فعال نمی‌شود.
    ],

    // «حرفه‌ای» — امکانات استاندارد + مدیریت اسناد
    'pro' => [
        'core',
        'announcements',
        'polls',
        'reserve',
        'meeting',
        'documents',
        // اگر واقعاً می‌خواهید «حسابداری عمومی» داشته باشید، اینجا (نه پیشرفته) را اضافه کنید:
        // 'accounting-general',
        // ⚠️ هرگز accounting-advanced-qr را اینجا اضافه نکنید مگر سیاست فروش‌تان همین باشد.
    ],

    // ───────────────────────────────────────────────────────────
    // پکیج‌های اختصاصی حسابداری (فقط این‌ها «پیشرفته» را فعال می‌کنند)
    // ───────────────────────────────────────────────────────────

    // نام متداول برای پکیج پیشرفته
    'accounting-advanced' => [
        'accounting-advanced-qr',
    ],

    // اگر در سیستم فروش شما اسلاگ پکیج پیشرفته این است:
    'accounting-advanced-qr-package' => [
        'accounting-advanced-qr',
    ],

    // اگر نام دیگری برای پکیج پیشرفته دارید، عیناً اینجا تعریف کنید:
    // 'your-advanced-package-slug' => ['accounting-advanced-qr'],

];
